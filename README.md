```html
<h1>Movie Rating App</h1>
<p>Pull down the repo and in terminal navigate to the root directory.</p>
<p>Run 'npm install' and 'bower install' in terminal to get all the required dependencies for package.json and bower.json</p>
```